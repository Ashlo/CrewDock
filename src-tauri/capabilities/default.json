{
  "identifier": "default",
  "description": "Default capability for the CrewDock desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default"
  ]
}
